@model CodeHollow.FeedReader.Feed

<rfc-inline-style css-file="Podcasts/_PodcastEpisodes.css"></rfc-inline-style>

<div class="podcast-episodes container">
    <div class="intro">
        <h2 class="text-center">Episodes</h2>
    </div>
    <div class="row">

        @if (Model != null && Model.Items != null && Model.Items.Count > 0)
        {
            foreach (var item in Model.Items)
            {
                <div class="blog-tile col-xs-12">
                    <a href="/podcasts/@RedFolder.Utils.SafeUrl.MakeSafe(item.Title)">
                        <hr />
                        <h3>@item.Title</h3>
                        <hr />
                        <p class="blog-tile-date">Published: @item.PublishingDateString</p>
                        <p>@Html.Raw(item.Description)</p>

                        <p>
                            Listen now&nbsp;&nbsp;<i class="fa fa-chevron-circle-right"></i>
                        </p>
                    </a>
                </div>
            }
        }
    </div>
</div>
